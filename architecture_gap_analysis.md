# 설계문서 대비 구현 점검 보고서 (Gap Analysis)

**점검일**: 2026-09-21 / **조치 반영**: 2026-09-21
**기준 문서**: `01_시민개발자_API포털_아키텍처정의서.md`(SA-ARCH-CDP-001), `02_시민개발자_API포털_구현상세스펙.md`(SA-SPEC-CDP-002)
**점검 대상**: `apisix` 브랜치 현재 상태
**점검 방법**: 문서 절별 코드 대조 + docker-compose 실기동 런타임 검증(감사로그 적재량·헤더·응답코드 실측)

> ## ✅ 조치 완료 현황 (2026-09-21)
>
> 아래 항목은 본 보고서 작성 후 **수정 완료**되었습니다. 나머지 항목은 미조치 상태로 유효합니다.
>
> | 항목 | 조치 내용 | 검증 |
> | :-- | :-- | :-- |
> | **GAP-01** 감사로그 유실 | `pat-audit.lua`를 자기재등록 `ngx.timer.at` 체인 기반 워커별 주기 플러시로 전환. 워커 종료(`premature`) 시 잔여분 best-effort 전송 | 무트래픽 상태에서 **91/91 전량 적재**(이전 42/91), 게이트웨이 재시작 후에도 유실 0 |
> | **GAP-02** TXS 무인증 | `X-Internal-Key` 공유 시크릿(상수시간 비교) + `allowed_cidr` 실검증 2중화 | SEC-05 테스트 추가, 키 없이 호출 시 403 CDP-1005 |
> | **GAP-03** `/internal/*` 무인증 | 라우터 레벨 `require_internal_key` 의존성 적용 | SEC-06 테스트 추가, PAT 메타 열람·감사로그 위조 모두 403 |
> | **GAP-04** dev-login | `ENABLE_DEV_AUTH`(기본 false)로 게이팅, 활성 시 기동 경고 로그 | 로컬 compose에서만 활성 확인 |
> | **GAP-05** 파티션 시한폭탄 | `audit_log_default` 안전망 + `BAT-CDP-06`(매월 1일, +2개월 선생성) 구현 | 2027-03 INSERT가 DEFAULT로 안착, 날짜 주입 테스트로 `audit_log_2027_02` 생성 확인 |
> | **R-08** 승인자 Scope 임의 지정 | 승인 API에서 `grantedScopes` 입력 제거. 부여 Scope = 신청 Scope ∩ 카탈로그 정의(서버 계산). 신청 단계에서도 미정의 Scope 차단 | TC-P-04/05 추가, 위조 `grantedScopes` 무시 확인 |
> | **UC-01** OpenAPI 스펙 | 카탈로그 등록/수정 시 `openapiSpec` 업로드·저장·조회 (뷰어는 범위 외) | 등록→조회 왕복 확인 |
> | **UC-07** 응답시간·오류율 | `BAT-CDP-01`이 avg/p95 지연 집계, usage API가 `errorRate`·`summary` 반환, 대시보드에 지표·추이 표시 | 오류율 20%·avg 6ms·p95 4ms 실측 |
> | **UC-10 / R-12** | 관리자 PAT 조회(X일내 만료·발급자·사용자ID 리스트) + 일괄 회수(선택/사용자단위/만료분) API 및 `/cdp/admin/tokens` 화면 | 조회·일괄회수·권한거부 동작 확인 |
>
> **부수 수정**: UC-07 오류율 집계를 위해 `pat-auth.lua`가 PAT 파싱 직후 `token_id`/`client_ip`/`trace_id`를
> 컨텍스트에 설정하도록 변경했습니다. 이전에는 모든 검증 통과 후에만 설정되어 거부 이벤트의 `token_id`가
> NULL이었고, 그 때문에 일별 오류율 집계에서 누락되고 탈취 시도 추적도 불가능했습니다(GAP-11의 절반이
> 함께 해소 — 다만 **오류 응답의 `X-Request-Id` 헤더는 여전히 미구현**이므로 GAP-11 자체는 유효).
>
> 회귀 테스트: 기존 19건 + 신규 4건(SEC-05, SEC-06, TC-P-04, TC-P-05) = **23/23 통과**.

---

## 0. 요약

런타임 핵심 경로(PAT 인증 → 쿼터 → 토큰교환 → 프록시)는 **동작하며 통합테스트 19건이 통과**합니다.
다만 **"동작한다"와 "설계문서를 충족한다"는 상당히 다릅니다.**

| 구분 | 완전 충족 | 부분 충족 | 미충족 |
| :-- | :--: | :--: | :--: |
| 유스케이스 (UC-01~13) | 5 | 6 | 2 |
| 요구사항 (R-01~12) | 4 | 5 | 3 |

**즉시 조치가 필요한 P0 이슈 5건**을 아래 §3에서 다룹니다. 그중 2건(감사로그 유실, 게이트웨이 지표 소실)은
**이번 APISIX 전환 과정에서 새로 생긴 회귀**이며, 나머지는 기존부터 존재하던 공백입니다.

가장 중요한 한 줄 요약: **"감사 로그가 전수 기록되지 않습니다."** 실측 결과 권한 거부 이벤트 91건 중
42건(46%)만 DB에 적재되었고, 나머지 49건은 게이트웨이 재시작과 함께 영구 소멸했습니다. R-09는 필수
요구사항이고 감사 로그는 이 시스템의 존재 이유 중 하나이므로, 이 항목이 단일 최우선 과제입니다.

---

## 1. 유스케이스 커버리지 (01 §3)

| UC | 명칭 | 상태 | 비고 |
| :-- | :-- | :-- | :-- |
| UC-01 | API 카탈로그 조회 | ⚠️ 부분 | 목록/상세는 동작. **OpenAPI 스펙 열람 불가**(`openapi_spec` 항상 null, Swagger UI 미탑재) |
| UC-02 | API 사용 신청 | ⚠️ 부분 | **자격 미충족도 신청이 통과됨**, `requestedScopes` 미저장, **결재 알림 발송 없음** |
| UC-03 | 신청 승인/반려 | ⚠️ 부분 | **부여 Scope가 카탈로그 정의 범위인지 검증 안 함**, `REJECTED_INELIGIBLE` 상태 없음 |
| UC-04 | PAT 발급/재발급 | ⚠️ 부분 | 발급은 스펙 충족. **"재발급" 전용 흐름 없음**, 최대 180일 예외승인 경로 없음 |
| UC-05 | PAT 폐기 | ✅ 충족 | 본인/관리자 폐기 + Redis 즉시 무효화 동작 확인 |
| UC-06 | Scope·쿼터 설정 | ⚠️ 부분 | 백엔드 API는 있으나 **관리 UI(`/cdp/admin/tokens`)가 없음**. API 오너는 쿼터 변경 불가(관리자 전용). **동시성(concurrency) 한도는 저장만 되고 집행 안 됨** |
| UC-07 | 사용량 대시보드 | ⚠️ 부분 | 호출수/쿼터는 표시. **응답시간·오류율이 항상 0**(배치가 latency를 집계하지 않음) |
| UC-08 | API 공개 등록 | ✅ 충족 | 카탈로그 등록 → APISIX 라우트 자동 생성까지 동작(금번 구현) |
| UC-09 | 감사 로그 조회 | ⚠️ 부분 | 조회 API·화면은 존재. **`auditor` 역할을 가진 사용자가 시스템에 없어 감사자 경로가 실행된 적 없음** |
| UC-10 | PAT 만료/미사용 회수 | ⚠️ 부분 | 만료(BAT-03)·미사용(BAT-04)은 동작. **만료 사전알림(BAT-02)·퇴직자 강제폐기(BAT-05, 문서상 "필수") 미구현** |
| UC-11 | PAT 기반 API 호출 | ✅ 충족 | 전 구간 동작, 통합테스트 통과 |
| UC-12 | PAT→JWT 토큰 교환 | ✅ 충족 | RFC 8693 교환·캐시·서킷브레이커 동작 |
| UC-13 | 쿼터·Rate Limit 집행 | ✅ 충족 | 원자적 차감 검증 완료(동시 20건 중 정확히 10건 통과) |

**액터 관점 공백**: 5개 액터 중 **보안 감사자(Auditor)는 실사용이 불가능**합니다. 실 Keycloak(`hd` realm)에
로드된 사용자 4명 어느 누구도 `auditor` 역할을 갖고 있지 않아(`infra/keycloak/hd-realm.json`은 목업이 정의했던
사용자 세트를 그대로 옮겨왔습니다), UC-09의 감사자 경로는 한 번도 실행된 적이 없습니다.

---

## 2. 요구사항 커버리지 (01 §2.2)

| R | 요구사항 | 상태 | 근거 |
| :-- | :-- | :-- | :-- |
| R-01 | 카탈로그 조회 + OpenAPI 스펙 열람 | ⚠️ | 스펙 업로드 필드·뷰어 모두 없음 |
| R-02 | 사용 신청 → 승인 워크플로우 | ⚠️ | 워크플로우는 동작하나 자격검증이 차단하지 않음, 알림 없음 |
| R-03 | PAT 발급/재발급/폐기, 원문 1회 노출 | ⚠️ | 재발급 흐름 부재 외 충족 |
| R-04 | PAT별 Scope·TPS·Daily/Monthly 쿼터 | ⚠️ | 동시성 한도 미집행, 관리 UI 부재 |
| R-05 | 사용량 통계 대시보드 | ⚠️ | 응답시간·오류율 미집계 |
| R-06 | PAT→JWT 교환(RFC 8693) | ✅ | |
| R-07 | PAT 해시 저장, 평문 미보관 | ✅ | SHA256 단방향(2026-09-21 변경), 로그 마스킹 동작 |
| R-08 | 최소 권한 (IdP 권한 ∩ API Scope) | ❌ | **교집합 강제 없음**. 승인자가 임의 Scope 문자열을 부여하면 그대로 PAT에 실림 |
| R-09 | 전 호출 감사 로그 (4중 상관관계) | ❌ | **전수 기록 실패**(§3.1). 실패 경로는 `trace_id`/`jwt_jti`가 NULL |
| R-10 | Gateway P95 ≤ 50ms | ❓ | **측정된 적 없음**(k6 시나리오 미작성) |
| R-11 | 전 구성요소 오픈소스 | ✅ | |
| R-12 | 만료 사전알림, 미사용 자동 비활성화 | ⚠️ | 비활성화만 구현, 알림 미구현 |

---

## 3. P0 — 즉시 조치 (5건)

### GAP-01. 감사 로그가 전수 기록되지 않음 ⛔ *(금번 전환에서 발생한 회귀)*

**증상 (실측)**: 권한 거부 요청 **91건 중 42건(46%)만 적재**. 게이트웨이 재시작 후에도 42건 그대로 —
미전송분 49건은 **영구 소멸**.

**원인**: `services/apisix-gateway/plugins/apisix/plugins/pat-audit.lua:95-105`. 배치 플러시 조건을
**"다음 요청이 도착한 시점"에만 검사**합니다.

```lua
if #_batch >= batch_size or (now - _last_flush) > flush_interval then ... end
```

APISIX는 nginx 워커 프로세스가 다수(코어 수만큼)이고 각 워커가 **자기 로컬 배치 테이블**을 가집니다.
요청이 워커에 분산되면 각 워커에 이벤트가 1~2건씩 갇히고, **그 워커에 다음 요청이 올 때까지 전송되지
않습니다.** 트래픽이 멈추면 무기한 대기, 재시작하면 소멸합니다.

스펙 §6.5의 "최대 100건 / **5초**"는 주기적 타이머 플러시를 전제로 한 표현인데, 구현에는 타이머가 없습니다.
구 FastAPI 게이트웨이에는 shutdown 훅(`flush_all`)이 있어 종료 시 잔여분을 비웠으나 이 역시 사라졌습니다.

**영향**: R-09 미충족. 특히 **산발적인 공격 시도(권한 없는 호출)가 정확히 기록되지 않는** 패턴이라
보안 감사 목적 자체가 훼손됩니다. 사용량 집계(BAT-CDP-01)도 audit_log 기반이라 통계가 과소 집계됩니다.

**조치**: `ngx.timer.every(flush_interval, ...)`로 워커별 주기 플러시를 걸고, 종료 시 플러시를 추가.
또는 워커 간 공유가 필요하면 `ngx.shared.DICT` 기반으로 전환.

---

### GAP-02. TXS에 IP 화이트리스트가 없어 임의 사용자 임퍼소네이션 가능 ⛔

`services/token-exchange-service/config.py:22`에 `allowed_cidr`가 **선언만 되어 있고 `main.py`에서 전혀
참조되지 않습니다**(참조 0건). 스펙 §5.1은 "Gateway Pod IP 허용목록 또는 mTLS"를 요구합니다.

**영향**: zone2-net 내 임의 컨테이너가 `{tokenId, userSub, scopes}` 세 값만으로 **PAT 검증을 건너뛰고
임의 사용자를 사칭하는 내부 JWT를 직접 발급**받을 수 있습니다. 게이트웨이의 PAT 인증 전체를 우회하는
권한 상승 경로입니다. (`plan.md` 2.7은 이 검증을 완료로 표기 — 허위 표기)

---

### GAP-03. 게이트웨이 전용 내부 API가 완전 무인증 ⛔

`services/portal-backend/routers/internal.py`의 `GET /internal/pat/{id}`, `POST /internal/audit`에
**인증·IP 제한이 전혀 없습니다.**

**영향**: (a) 누구나 tokenId만 알면 PAT 메타(사용자, Scope, 쿼터)를 열람, (b) **누구나 임의 감사 로그를
위조 삽입** 가능 — 부인방지(Repudiation) 통제가 무력화됩니다. 포트 8080이 호스트에 게시되어 있어
컨테이너 밖에서도 접근 가능합니다.

---

### GAP-04. 운영 배포 시 인증을 무력화하는 개발용 엔드포인트 ⛔

`services/portal-backend/routers/dev_auth.py`의 `POST /portal/v1/auth/dev-login`은 **사용자 ID를 지정하는
것만으로 실제 서명된 JWT를 발급**합니다. 스펙 §7.5의 "SSO + PKCE, 프론트는 토큰 미접근" 전제와 정면으로
배치되며, 빌드 플래그·환경 분기 없이 항상 활성화되어 있습니다. 운영 이관 전 제거 또는 환경 게이팅 필수.

---

### GAP-05. 2027-02-01부터 감사 로그 적재가 전면 실패 (시한폭탄) ⛔

`infra/postgres/init/01_ddl.sql:107-116`은 `audit_log` 파티션을 **2027-02-01까지만** 생성하고
**DEFAULT 파티션도 없습니다.** 월별 파티션 생성 배치(BAT-CDP-06)는 미구현입니다.

**영향**: 2027-02-01 00:00부터 모든 `INSERT INTO cdp.audit_log`가 예외로 실패 → 감사 로그 전량 유실 +
PAT 발급/폐기 API도 감사 INSERT를 포함하므로 **동반 실패**할 수 있습니다.

---

## 4. P1 — 높음 (8건)

| ID | 갭 | 위치 / 근거 |
| :-- | :-- | :-- |
| GAP-06 | **최소권한 미강제(R-08)**: 승인 시 `grantedScopes`가 해당 API의 `api_scope` 정의에 속하는지, 신청자가 요청한 Scope인지, IdP 권한과 교집합인지 **전혀 검증하지 않음**. 임의 문자열도 그대로 PAT에 부여 | `routers/applications.py:126-131` |
| GAP-07 | **자격 미충족 신청이 그대로 접수됨**: `missingRoles`를 계산해 응답에만 담고 PENDING으로 생성. UC-02 대안흐름의 즉시 반려(`REJECTED_INELIGIBLE`) 미구현이며 해당 상태값 자체가 DDL CHECK에 없음. `requestedScopes`도 저장되지 않아 승인자가 신청 내용을 볼 수 없음 | `routers/applications.py:55-69` |
| GAP-08 | **프론트엔드가 BFF 패턴이 아님**: JWT를 **localStorage에 저장하고 Authorization 헤더로 직접 전송**. 스펙 §7.5의 "HttpOnly·Secure 쿠키, 프론트 JS는 토큰 접근 불가"와 정반대이며 XSS 시 토큰 탈취 가능. CSRF(`X-XSRF-TOKEN`) 처리도 없음 | `frontend/portal-web/src/api/axios.ts:11-17`, `stores/auth.ts:27` |
| GAP-09 | **게이트웨이 Prometheus 지표 3종 소실** *(금번 전환 회귀)*: `cdp_api_requests_total`, `cdp_api_latency_seconds`, `cdp_quota_rejected_total`을 구 FastAPI 게이트웨이는 기록했으나 커스텀 Lua 플러그인은 어떤 지표도 노출하지 않음. 추가로 APISIX `prometheus` 플러그인이 **활성화만 되고 어떤 라우트/global_rule에도 부착되지 않아** 요청 단위 지표도 수집되지 않음 | `plugins/apisix/plugins/*.lua`, `conf/config.yaml:43`, `apisix_client.py` |
| GAP-10 | **BAT-CDP-05(퇴직·휴직자 PAT 강제 폐기) 미구현** — 문서 §9에서 유일하게 "필수, 실패 시 즉시 알림"으로 지정된 배치. 퇴직자의 PAT가 만료일까지 유효하게 남음 | `batch.py` (01/03/04만 존재) |
| GAP-11 | **오류 경로에 trace_id가 없음**: `ctx.cdp_trace_id`를 pat-auth의 모든 검증 **통과 후에** 생성하므로 401/403/429 응답에 `X-Request-Id`가 없고(실측 확인), 감사로그 `trace_id`도 NULL. 스펙 §10.3의 "오류 응답에 포함하여 CS 대응" 미충족이며, 탈취 의심 건 추적이 불가능 | `pat-auth.lua:250`, `common.lua`(problem_json) |
| GAP-12 | **관리자 PAT 관리 화면(`/cdp/admin/tokens`) 부재**: `PUT /tokens/{id}/quota`(R-04/UC-06)를 호출할 UI가 전무. 또한 라우트 가드가 로그인 여부만 확인하고 **역할 검사를 하지 않아** URL 직접 입력으로 결재함·감사로그 화면 진입 가능(실제 차단은 백엔드 의존) | `frontend/src/router/index.ts:13-37` |
| GAP-13 | **`cdp_pat_id` 클레임이 운영에서 누락될 구조**: TXS가 이를 토큰 요청 **form 파라미터**로 보내는데, 실제 Keycloak은 미인식 파라미터를 무시함. 스펙 §5.3은 Protocol Mapper 주입을 요구. 내부 GW/PDP가 이 클레임으로 시민 채널을 식별한다면 운영 전환 시 동작하지 않음 | `token-exchange-service/keycloak_client.py:49` |

---

## 5. P2 — 중간 (11건)

| ID | 갭 | 비고 |
| :-- | :-- | :-- |
| GAP-14 | 알림 채널 자체가 없음 (메일/메신저) — 결재 요청, 만료 D-7/D-1, 배치 실패 알림 모두 불가 | 코드베이스 전체 검색 0건 |
| GAP-15 | 사용량 대시보드 **응답시간·p95가 항상 0** — BAT-CDP-01이 `avg_latency`/`p95_latency`를 집계하지 않음 | R-05 부분 미충족 |
| GAP-16 | **동시성 한도 미집행** — `quota_policy.concurrency`와 `cdp:cc:{tokenId}` 키가 게이트웨이에서 사용되지 않는 死데이터 | 스펙 §2.2 |
| GAP-17 | **알람 규칙 6종 전무** — `prometheus.yml`에 `rule_files` 자체가 없고 alertmanager 미배포 | 스펙 §10.2 |
| GAP-18 | **분산 추적(OpenTelemetry/W3C traceparent) 미구현** | 스펙 §10.3 |
| GAP-19 | **성능 테스트(k6) 미작성** — R-10(P95 50ms), 2,000 TPS 등 비기능 목표가 검증된 적 없음 | 스펙 §11.3 |
| GAP-20 | **Grafana 대시보드 부재** — `infra/grafana/` 디렉터리가 존재하지 않음에도 `plan.md` 8.3은 완료로 표기 | 허위 완료 표기 |
| GAP-21 | **RFC 9457 위반 12곳** — `raise HTTPException(...)` 경로가 `{"detail":...}` 형태로 나가 `type/title/status/instance` 누락. 전역 예외 핸들러 미등록 | 스펙 §4.3 |
| GAP-22 | TXS **재시도 없음**(설정값 死데이터), **HALF_OPEN 상태 전이 없음**(30초 후 전면 개방), Bulkhead 없음 | 스펙 §5.4 |
| GAP-23 | **INACTIVE→ACTIVE 복귀 경로 없음** — 자동 회수된 PAT는 관리자도 되살릴 수 없음. `revoke_reason`도 기록 안 함 | 스펙 §3.3 |
| GAP-24 | **MFE Module Federation 미구현** — `remoteEntry`·shared 싱글톤 부재로 Shell 통합 및 RK-04 리스크 검증 불가. 현재는 독립 SPA | 스펙 §7.2 |

---

## 6. P3 — 낮음 / 관찰 사항

- **콜드 스타트 시 서비스 공백**: etcd가 비어 있는 상태에서 게이트웨이가 먼저 기동되면, portal-backend의
  라우트 재동기화가 끝날 때까지(실측 약 10초) 전 시민 API가 404를 반환합니다. 롤링 배포 시 순간 장애 가능.
- **PAT 발급 다이얼로그**: ESC·배경 클릭 차단 로직 없음, 클립보드 실패 시 `alert()`로 평문 노출
  (스펙 §7.4 요건 3·7 부분 미충족).
- **미매칭 경로 오류 포맷**: `/capi/v1/*` 미등록 경로는 `CDP-2002`가 아닌 APISIX 기본 404를 반환.
- **Zone2→Zone3 mTLS 미적용**(평문 HTTP), `auth.require_role()` 死코드, `unlock_if_mine.lua` 미사용,
  `PUT /tokens/{id}/quota`의 월별 쿼터 TTL 표기 차이(기능 영향 없음).
- **부록 C.8 PoC 판정 체크리스트 9항목 중 8항목 미검증** — 특히 다운스코핑 실증, 내부 GW 무변경 수용 검증이
  공백이라 **운영 Keycloak 전환 리스크가 정량화되지 않은 상태**입니다.
  2026-09-26 로컬 컴포즈의 Zone 4를 실 `quay.io/keycloak/keycloak:24.0.0`으로 교체하면서
  실 Keycloak에서 RFC 8693 `requested_subject` 임퍼소네이션이 성립하는 것 자체는 검증되었으나(realm-import된
  `hd` realm, `citizen-gw-exchanger`에 `impersonation` 롤 부여), scope 파라미터를 좁혔을 때 발급 토큰의 `scope`
  클레임이 실제로 줄어드는지에 대한 실증은 여전히 미확인입니다. 또한 dev realm은 audience 클라이언트에
  fine-grained token-exchange 권한 정책을 설정하지 않았기 때문에 TXS가 `audience` 파라미터를 보내지 않는
  형태(대신 audience protocol mapper로 `aud=internal-api-gateway` 강제)로 우회했습니다 — 운영 realm에서는
  이 정책을 명시적으로 켜고 audience 파라미터를 다시 활성화하는 방향으로 정리해야 합니다.

---

## 7. 문서 자체의 정합성 문제

점검 과정에서 **문서가 구현보다 앞서 있다고 표기된 항목**을 발견했습니다. 계획 문서를 신뢰해 의사결정하면
위험하므로 별도로 기록합니다.

| 위치 | 표기 | 실제 |
| :-- | :-- | :-- |
| `plan.md` 2.7 | TXS IP 화이트리스트 검증 `[x]` | 미구현 (GAP-02) |
| `plan.md` 8.3 | `infra/grafana/dashboards/cdp_overview.json` `[x]` | 파일·디렉터리 없음 (GAP-20) |
| `plan.md` 7.8 | 보안 테스트 4/4 통과 | 권한 상승(내부 경로 호출 차단) 테스트 없음 |
| `01` §3.3 UC-11 | 토큰 교환 실패 → `502` | `02` §4.3은 `CDP-2001 = 503`. 구현은 503. 문서 간 불일치 |
| `01` §6 | `PAT_SCOPE` 테이블 | `02` §2.1에서 `pat.scopes TEXT[]`로 대체됨(구현은 02를 따름) |

---

## 8. 권고 조치 순서

1. **감사 로그 신뢰성 회복** (GAP-01) — 주기 타이머 플러시 + 종료 시 플러시. 이것 없이는 R-09·보안감사·
   사용량 통계가 모두 성립하지 않습니다.
2. **내부 신뢰 경계 차단** (GAP-02, GAP-03, GAP-04) — TXS IP 제한, `/internal/*` 인증, dev-login 게이팅.
   세 건 모두 인증 체계를 통째로 우회하는 경로입니다.
3. **파티션 시한폭탄 해제** (GAP-05) — DEFAULT 파티션 추가(즉효) + BAT-CDP-06 구현(항구).
4. **최소권한 강제** (GAP-06, GAP-07) — 승인 Scope ⊆ 카탈로그 Scope ∩ 신청 Scope ∩ IdP 권한 검증.
5. **관측성 복구** (GAP-09, GAP-11) — 지표 3종 재노출, prometheus 플러그인 라우트 부착, trace_id를
   pat-auth 진입 시점으로 이동.
6. 이후 P1 잔여(GAP-08 BFF, GAP-10 BAT-05, GAP-12 관리 UI, GAP-13 클레임) → P2 순.

> **범위 판단 참고**: GAP-08(BFF), GAP-24(MFE), 부록 C 계열은 "사내 표준 인프라와 결합해야 완성되는"
> 항목이라, 현 목업 환경에서 단독으로 완결시키기 어렵습니다. 운영 이관 계획과 묶어 판단하는 편이
> 합리적입니다. 반면 §8의 1~5번은 현 환경에서 즉시 해소 가능하며, 그중 1·2·3번은 보안·데이터 무결성에
> 직결되므로 다음 작업으로 권장합니다.
